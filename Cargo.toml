[package]
name = "helios"
version = "0.1.0"
edition = "2021"
authors = ["Sol"]
description = "Helios — an OS where everything is a memory"
build = "build.rs"

[[bin]]
name = "helios"
path = "src/main.rs"

[profile.release]
opt-level = 2
lto = true
panic = "abort"

[profile.dev]
panic = "abort"
